<section class='small-10 columns'>
  <div class='mainheader'> Accesses </div>
  <div class='subdescription'> List of access logs within the system. </div>
  <hr>
  <%= generic_table_search %>
  <% table_orientation = shift_table_orientation %>
  <% begin %>
      <table class='generic_table' id='permission_table'>
        <thead>
        <tr>
          <th>
            ID
            <%= generic_table_theadlink('accesses.id', table_orientation) %>
          </th>
          <th>
            Actor ID
            <%= generic_table_theadlink('accesses.actor_id', table_orientation) %>
          </th>
          <th>
            Username
            <%= generic_table_theadlink('accesses.username', table_orientation) %>
          </th>
          <th>
            Email
            <%= generic_table_theadlink('accesses.email', table_orientation) %>
          </th>
          <th>
            Created At
            <%= generic_table_theadlink('accesses.created_at', table_orientation) %>
          </th>
          <th>
            Last Updated
            <%= generic_table_theadlink('accesses.updated_at', table_orientation) %>
          </th>
          <th>
            Last Login
            <%= generic_table_theadlink('accesses.last_login', table_orientation) %>
          </th>
          <th class="actions" align="center">&nbsp;</th>
        </tr>
        </thead>
        <% if @accesses.empty? == true %>
            <% raise exception %>
        <% else %>
            <tbody>
            <% @accesses.each do |access| %>
                <tr>
                  <td><%= access[:id].upcase %></td>
                  <td><%= access[:actor_id] %> </td>
                  <td><%= access[:username] %> </td>
                  <td><%= access[:email] %> </td>
                  <td><%= access[:created_at] %> </td>
                  <td><%= access[:updated_at] %> </td>
                  <td><%= access[:last_login] %> </td>
                  <td class="actions">
                    <%= generic_table_actions(access[:id]) %>
                  </td>
                </tr>
            <% end %>
            </tbody>
        <% end %>
      </table>
  <% rescue => ex %>
      <%= generic_table_error(ex) %>
  <% end %>
  <%= generic_table_footer(@accesses) %>
</section>