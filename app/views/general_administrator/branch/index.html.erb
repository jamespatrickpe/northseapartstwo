<section class='small-10 columns'>
  <%= generic_title('Branches','Location-wise Business Subdivisions') %>
  <% table_orientation = shift_table_orientation %>
  <% begin %>
      <table class="generic_table" id="holidays_table">
        <thead>
        <tr>
          <%= generic_table_theadlink('Branch ID','branches.id', table_orientation) %>
          <%= generic_table_theadlink('Branch Name','branches.name', table_orientation) %>
          <%= generic_table_theadlink('Date Created','branches.created_at', table_orientation) %>
          <%= generic_table_theadlink('Last Updated','branches.updated_at', table_orientation) %>
          <th class="actions" align="center">&nbsp;</th>
        </tr>
        </thead>
        <% if @branches.empty? == true %>
            <% raise exception %>
        <% else %>
            <tbody>
            <% @branches.each do |branch| %>
                <tr>
                  <td><%= branch[:id].upcase %></td>
                  <td><%= branch[:name] %></td>
                  <td><%= branch[:created_at] %></td>
                  <td><%= branch[:updated_at] %></td>
                  <td class="actions">
                    <%= generic_table_actions(branch[:id]) %>
                  </td>
                </tr>
            <% end %>
            </tbody>
        <% end %>
      </table>
  <% rescue => ex%>
      <%= generic_table_error(ex) %>
  <% end %>
  <%= generic_table_footer(@branches) %>
</section>