<%=
    begin

      extract_polymorphic_attribute(current_model_instance.class);

      representation_hash = current_model_instance.main_representation
      link_to representation_hash[:attribute],representation_hash[:controller_path]

    rescue => ex
      " N/A " + ex.to_s
    end
%>